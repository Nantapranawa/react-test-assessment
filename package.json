{
  "name": "tes-nextjs-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "backend"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@repo/web",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  }
}